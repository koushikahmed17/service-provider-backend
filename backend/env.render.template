# Render.com Environment Variables Template
# Copy these to your Render service's Environment Variables section

# App Configuration
NODE_ENV=production
PORT=10000
API_PREFIX=api/v1

# Database (Use Internal Database URL from Render)
DATABASE_URL=postgresql://username:password@hostname/database_name

# JWT Configuration (Generate using: node scripts/generate-secrets.js)
JWT_SECRET=your-generated-jwt-secret-here
JWT_EXPIRES_IN=7d

# CORS (Update with your frontend URL)
CORS_ORIGIN=https://your-frontend-domain.com

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# Security (Generate using: node scripts/generate-secrets.js)
BCRYPT_ROUNDS=12
CSRF_SECRET=your-generated-csrf-secret-here

# Logging
LOG_LEVEL=info

# Email (Optional - Only if you have SMTP configured)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# SMS (Optional - Only if you have SMS service)
SMS_PROVIDER=
SMS_API_KEY=
SMS_API_SECRET=






